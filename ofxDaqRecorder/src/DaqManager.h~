#include "ofMain.h"
#include "ofxXmlSettings.h"

class DaqManager : public ofThread {

    public:

        DaqManager();
        ~DaqManager();
        bool initialize(ofFilePath settingsFile);
        bool start();
        bool stop();
        void query();

    private:

        vector<DataStream> dataStreams;
        ofxXmlSettings settings;
}
